package com.pop.rules.repositories;

import com.pop.rules.entities.ComplexCondition;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ComplexConditionRepository extends JpaRepository<ComplexCondition, Long> {
}
