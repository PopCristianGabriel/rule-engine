package com.pop.rules.repositories;

import com.pop.rules.entities.SimpleCondition;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SimpleConditionRepository extends JpaRepository<SimpleCondition, Long> {
}
