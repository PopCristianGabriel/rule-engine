package com.pop.rules.enums;

public enum LogicalOperator {
    // Logical
    AND,
    OR,
    XOR,

    // Comparison
    EQUALS,
    NOT_EQUALS,
    GREATER_THAN,
    LESS_THAN,
    GREATER_THAN_OR_EQUAL,
    LESS_THAN_OR_EQUAL
}
